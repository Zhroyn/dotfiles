[user]
	name = Pxtkn
	email = hr.zheng@outlook.com
[alias]
	co = checkout
	br = branch
	st = status
	ci = commit
	aa = add .
	cg = config --global
	cf = cat-file -p
	lf = ls-files -s
	re = remote
	cia = commit -am
	logg = log --pretty=format:'%h %cd %s' --graph
[merge]
	tool = code
[difftool "code"]
	cmd = code --wait --diff $LOCAL $REMOTE
[mergetool "code"]
	cmd = code --wait $MERGED
[diff]
	tool = code
